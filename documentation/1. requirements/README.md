Problem, vision and scope for chat/directmessage/moderation as extracted from
mwanachama-backend-api-gateway. See the gateway's own
`documentation/1. requirements/` for the original product requirements this
domain implements (chapter chat, direct/group messaging, message moderation)
— this file tracks only what changed by moving the code, not the product
requirements themselves.
