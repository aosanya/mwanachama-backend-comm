# mwanachama-backend-comm — completed work

| Task | Title | Completed | Notes |
|---|---|---|---|
| W1 | Scaffold the module | 2026-09-03 | Flat root package `mwanachamacomm` (module-suffix convention, matching `mwanachamataskmanager`), `go.mod`/`.gitignore`/`Makefile`/`README.md`/`CLAUDE.md`, four-phase `documentation/`. |
| W2 | Port chat/directmessage/moderation domain types | 2026-09-03 | `chat.go`, `chat_activity.go`, `directmessage.go`, `moderation.go`, `moderation_repository.go` — unchanged in shape; collision-prone names (`Thread`, `Message`, `Repository`, `ErrNotFound`) prefixed by domain (`Chat*`/`DM*`/`Moderation*`/`ErrModerationNotFound`) once flattened into one package. See CLAUDE.md. |
| W3 | Design and port the custody dependency inversion | 2026-09-03 | `moderation_act.go`'s `ActEntry`/`ActKind`/`ActWriter`/`MemoryActWriter`, replacing the direct `internal/domain/custody` import moderation's act.go could no longer make. Validated against the gateway's `internal/domain/custody/act_writer_census_test.go` (DEV-1257) via `custodyKindOf`'s explicit per-kind mapping in the gateway's `comm_act_writer.go`. |
| W4 | Port both store backends | 2026-09-03 | `store_postgres_*.go` (chat/dm/moderation, `comm_`-prefixed table names) and `store_memory_*.go`, plus their ported unit tests — all passing. |
| W5 | Build the postgres scratch-test schema and live suite | 2026-09-03 | `schema.sql` (squashed, comm_-prefixed, two deliberate divergences from production — see CLAUDE.md) and `postgres_scratch_test.go`, run successfully against a real Postgres instance during development (idempotent mint, DM lifecycle, moderation act-log-in-same-transaction including rollback-on-failure, cross-backend participant-order agreement). |
| W6 | Rewire the gateway to import this module | 2026-09-03 | `cmd/server/stores.go`/`comm_act_writer.go`, `internal/api/http/deps.go`/`errors.go`/`member_visibility.go`/`dm_eligibility.go` and all chat/dm/moderation handler files repointed at this module; old `internal/domain/{chat,directmessage,moderation}` and their store implementations deleted. Every HTTP route path is byte-identical to before the move. Full gateway test suite (memory backend) green. |
| W7 | Write the production rename migration | 2026-09-03 | `internal/store/postgres/migrations/000065_extract_comm_tables.{up,down}.sql` — pure `ALTER TABLE/SEQUENCE ... RENAME`, no data touched. Not yet applied to any real deployment (see todo.md). |
| W11 | Refactor onto the `mwanachama-backend-actor` template | 2026-09-04 | Storage moved from hand-rolled `pgx` SQL + a parallel memory store to one GORM-backed store per domain (`models/`+`gormstore/` split); a new `routes/` package covers the 19 of 37 gateway chat/dm/moderation routes that carry no gateway-internal policy. `schema.sql`/`postgres_scratch_test.go` retired in favor of `gormstore.Migrate` + `postgres_integration_test.go`. Supersedes W4/W5's store backends and scratch schema. See [the session write-up](coding_sessions/2026-09-04_actor-template-refactor.md) and CLAUDE.md. Gateway wiring left broken — see todo.md W12/W13. |
| W12 | Rewire the gateway's storage adapters to this module's new GORM constructors | 2026-09-14 | **Already done** by the gateway's own `todo_comm_absorb.md` board (DEV-1664…1669, completed 2026-09-06) — this row was simply never struck here. Verified fresh against disk, not against that board's word alone: the gateway's `cmd/server/stores.go` constructs `mwanachamacomm.NewChatStore`/`NewDMStore`/`NewModerationStore`/`NewAddressStore`/`NewNotificationStore` directly (no `ChatPostgresStore`/`DMMemoryStore`-style old adapters anywhere in the gateway repo), `go build ./...` is clean, and this repo's own `go build ./... && go vet ./... && go test ./...` is green. |
| W13 | Repoint the gateway's chat/DM/moderation handlers at W11's 19 `routes/` routes | 2026-09-14 | **Already done for 17 of 19**, verified against disk: `router.go`/`dm_handlers.go`/`address_handlers.go` loop over `commroutes.ChatActivity`/`DMRoutes`/`DMMessageRoutes`/`NotificationRoutes`/`ListMyAddresses`/`RetireAddress` (chat's 1 + DM's 16). The remaining 2 — moderation's `listReportQueue`/`listReportsForMessage` — are still hand-registered in `moderation_handlers.go` rather than looped from `commroutes.ModerationRoutes`, but both already call straight into `d.Moderation` (comm's GORM-backed `ModerationRepository`, the same store W12 wired) with no legacy store or duplicate business logic involved — a structural nit, not a functional gap. Full gateway build green; the two `internal/api/http` test failures seen while verifying (`TestPostmanCollectionCoversEveryRoute`, `TestPostmanCollectionHasNoOrphanRequests`) are both unrelated pre-existing issues — agency's AG12 routes missing Postman coverage (flagged in developer's compiled backlog) and a known coverage-test blind spot for loop-registered routes (filed as DEV-1688) — confirmed neither traces to chat/DM/moderation/comm. |
| W10 | Create the `aosanya/mwanachama-backend-comm` GitHub remote and push | 2026-09-14 | **Already done, this row simply never struck** — same pattern as W12/W13 above. Verified fresh against disk: `git remote -v` shows `origin` pointed at `https://github.com/aosanya/mwanachama-backend-comm.git`, `gh auth status` confirms an active, authenticated `aosanya` account with `repo` scope, `gh repo view aosanya/mwanachama-backend-comm` resolves the remote repository, and `git branch -vv` shows local `master` at `75a687a` tracking `origin/master` with no ahead/behind divergence — nothing left to push. |
| W14 | 🐞 A DM thread's last admin could strand every remaining participant admin-less; `Kick` had no self-kick guard (P1) | 2026-09-21 | Two new sentinels in `models/directmessage.go`, re-exported from `doc.go`: `ErrDMSelfKick` (`Kick` in `dm_roster_impl.go` refuses `memberID == by`) and `ErrDMLastAdmin` (`Leave` refuses when the leaver is an active admin, no other active admin exists, and at least one other participant is active). The row left the refusal shape as a design call; I chose new sentinels over reusing `ErrDMNotLastToLeave`, whose meaning is about re-enabling, and mapped both to `409` in `routes/dm.go`'s `dmStatusFor` (the state conflicts with the request) rather than the blanket `403` the unexported not-admin error gets. Rules as built: a successor must be promoted before the last admin leaves; a sole participant may still leave; a non-admin is unaffected; invited-but-not-accepted people do not count as active, so an admin may leave a thread whose only other members have not joined. Not done: no separate recovery path for a thread that is already admin-less from before this fix, and the check is a read then a write with no lock, so two admins leaving at once could still both pass it. The five pins in `routes/w14_dm_admin_lockout_test.go` and `..._httptest_test.go` went red as designed and were rewritten: self-kick refused, last-admin leave refused until a successor is promoted (then allowed and the successor can administer), and the two non-blocking cases. Verified: `go build ./... && go vet ./... && go test ./...` and `go test -tags=integration ./...` green, `gofmt -l .` clean. |

## Archived board context

### Why this repo exists

Extracted from mwanachama-backend-api-gateway's chat, directmessage and
moderation domains at the user's request, following the in-process-library
architecture mwanachama-backend-taskmanager set (not a network service — see
CLAUDE.md and the gateway's README.md). Scope (chat+directmessage+moderation,
not notification) and the `comm_` table-prefix convention were both decided
with the user before implementation began.
